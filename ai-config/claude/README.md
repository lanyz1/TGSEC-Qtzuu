# TGSEC 红队执行引擎 — Claude Code 配置

将以下内容作为 Claude Code 的 system prompt 或项目上下文使用。

## 安装方式

```bash
# 方式1: 项目级 CLAUDE.md(Claude Code 自动读取)
cp claude/CLAUDE.md ~/security-suite/CLAUDE.md

# 方式2: 添加到 Claude Code 设置
# 打开 Claude Code → Settings → Custom Instructions → 粘贴以下内容
```

## CLAUDE.md 内容

见同目录下 `CLAUDE.md` 文件。
