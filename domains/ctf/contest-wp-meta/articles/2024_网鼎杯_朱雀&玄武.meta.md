---
title: 2024 网鼎杯 朱雀 & 玄武
contest: 网鼎杯
year: 2024
difficulty: hard
vuln_type: [lattice, crypto_rsa, secret_sharing]
tags: [已知 p 高位 256/512 Coppersmith, 多层 RSA 套娃, p1q1=2024*p1, n22=p^2+q^2 解方程, Shamir 秘密共享 kernel 攻击]
attack_chain: 朱雀 crypto002: 已知 p 高 256bit → Coppersmith X=2^248 beta=0.49 epsilon=0.014 → 爆破 7 比特 p / crypto003: 3 层 RSA 套娃 - Layer1: n1=p1*q1 q1=next_prime(2024*p1) → n1//2024 开根还原 p1 / Layer2: n2=p2*q2, n22=p2^2+q2^2 → 解二元二次方程 (p2+q2)^2 = n22+2n2, (p2-q2)^2 = n22-2n2 / Layer3: p3/q3 共享 1024 高位 → 已知 p3 高位攻击 / 两式 m1=p1*m^2+p2*m+p3, m2=q1*m^2+q2*m+q3 → 解联立方程 / 玄武 Shamir: 10000 份 shares 每份 64 个 polynomial coefs + value, n_players=64, threshold=128 > 实际自由度 32 → 拆 32 维 kernel (C2 = matrix of high coefs) → 累积 C1*kernel 拼出完整矩阵 → 求解
key_payload: f = (p>>248)<<248 + x ; f.small_roots(X=2^248, beta=0.49, epsilon=0.014) ; n1 // 2024 开根 = p1 ; m^2 = p1*m1 - p2*m + p3 / p1 ; ShamirSS(coefs, value) 64 个 coefs, 10000 组
one_liner: Coppersmith 已知 p 高位 + 多层 RSA 套娃 + Shamir kernel 攻击。
lesson: Shamir 秘密共享若 threshold > 自由度，攻击者可取 C2 矩阵求 kernel 还原 C1 拼出完整方程组。
quality: high
---
# 2024 网鼎杯 朱雀 & 玄武

## 朱雀

### crypto002（已知 p 高 256 bit）

```python
p = getPrime(512); q = getPrime(512)
n = p * q
# 已知 p 的高 256 bit
R.<x> = Zmod(n)[]
f = (p >> 248) << 248 + x
f.small_roots(X=2**248, beta=0.49, epsilon=0.014)  # Coppersmith
```

爆破 7 bit (38 分钟) → 完整 p。

### crypto003（三层 RSA 套娃）

```python
p1 = getPrime(1024); q1 = next_prime(2024 * p1); n1 = p1*q1
# Layer 1: n1 // 2024 开根 = p1
p2 = getPrime(1024); q2 = getPrime(1024)
n2 = p2*q2; n22 = p2**2 + q2**2
# Layer 2: 解 (p2+q2)^2 = n22 + 2*n2
# (p2-q2)^2 = n22 - 2*n2
# p2 = (sqrt(p2+q2) + sqrt(p2-q2)) // 2
# 已知 p3 高 528 bit → Coppersmith
# 两式联立：
m1 = p1*m**2 + p2*m + p3
m2 = q1*m**2 + q2*m + q3
# m1 - m2 = (p1-q1)*m^2 + (p2-q2)*m + (p3-q3)  # 已知 mod n
```

**e = 2999** → 4 次 e 次方开根还原 m。

```python
# github.com/jvdsn/crypto-attacks 里有 fast_polynomial_gcd
# 用 p1, q1, p2, q2, p3, q3 之间的关系 + GCD 还原 m
```

## 玄武

### Shamir 秘密共享 kernel 攻击

```python
class ShamirProtocol:
    def __init__(self, n_players, threshold, modulus):
        self.n_players = n_players
        self.threshold = threshold
        self.modulus = modulus
    def input_share(self, values):
        coefs = [[getRandomRange(1, self.modulus) for _ in range(self.threshold)]
                 for _ in range(self.n_players)]
        randoms = values + [getRandomRange(1, self.modulus) for _ in range(self.threshold - len(values))]
        values = [sum([r*c for r, c in zip(randoms, coef)]) % self.modulus for coef in coefs]
        return [ShamirSS(coef, value) for coef, value in zip(coefs, values)]

protocol = ShamirProtocol(len(flag), len(flag) * 2, 257)
# len(flag) ≈ 32, threshold = 64 > 32
# 实际只有 32 维，但给了 64 个系数
```

**关键洞察**：threshold=64 但实际只 32 个未知数 → 多余 32 个方程是冗余的。

```python
p = 257
NEWV = []
FLAG = True
index = 0
for i in trange(10000):
    s = load('share_%d.sobj' % i)
    C1 = matrix(GF(p), [ss.coefs[:32] for ss in s])   # 真实 32 维
    C2 = matrix(GF(p), [ss.coefs[32:] for ss in s])   # 多余 32 维
    V = vector(GF(p), [ss.value for ss in s])
    if C2.rank() < 32:
        Ck = C2.left_kernel().basis()
        CK = Matrix(GF(p), Ck)
        newC1 = CK * C1
        newV = CK * V
        if FLAG:
            NEWC1 = newC1
            NEWV.append(newV)
            FLAG = False
            continue
        NEWC1 = NEWC1.stack(newC1)
        NEWV.append(newV)
        index += 1
NEWV = [i[0] for i in NEWV]
NEWV = vector(GF(p), NEWV)
FLAG = (NEWC1.solve_right(NEWV))
print(''.join(chr(i) for i in FLAG))
# 197de281fd12e95eba04e7b84539fc8c
```

**攻击**：
- `C2 * x = 0`（kernel）→ 用 kernel basis 投影 `C1` 和 `V`
- 把 `C1 * x = V` 投影到 kernel 空间
- 多次累积 `NEWC1` 矩阵，足够多时 `solve_right` 求出 `x`（32 维 flag）
