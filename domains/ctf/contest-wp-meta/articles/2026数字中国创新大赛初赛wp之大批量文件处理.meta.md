---
title: 2026 数字中国创新大赛初赛 wp 之大批量文件处理（docx 身份证号批量提取）
contest: 2026 数字中国创新大赛
year: 2026
difficulty: easy
vuln_type: [misc_unknown, web_unknown]
tags: [数字中国 2026 初赛, 大批量文件处理, docx 提取 word/document.xml, ET.parse iter t 标签, 身份证号正则 d{17}[dXx], 老旧 15 位 + 现代 18 位, 连续数字 + 分段存储空格短横, 字体颜色白色隐藏]
attack_chain:
  - 打开 word 的"显示/隐藏"选项看隐藏文本
  - docx = zip → word/document.xml → ET.parse iter t 标签
  - 拼接纯文本
  - 正则 d{17}[dXx] 18 位身份证
  - 老旧 15 位身份证
  - 分段存储情况：空格 / 短横
key_payload: "ET.parse iter t 标签 + re.finditer d{17}[dXx]"
one_liner: 2026 数字中国大批量文件处理：docx 提取纯文本 + 18 位身份证号正则批量提取 + 15 位老版兼容。
lesson: docx 实质是 zip + XML；批量提取 = zipfile + ET.parse + iter + 正则；身份证 18 位正则 d{17}[dXx] 是国内取证/数据安全赛事必考。
quality: medium
---

# 2026 数字中国创新大赛初赛 wp 之大批量文件处理

## 解题脚本

```python
import re
import zipfile
from pathlib import Path
from xml.etree import ElementTree as ET

def extract_text_from_docx(docx_path):
    text = ""
    try:
        with zipfile.ZipFile(docx_path) as docx_zip:
            with docx_zip.open('word/document.xml') as document_xml:
                tree = ET.parse(document_xml)
                root = tree.getroot()
                for elem in root.iter():
                    if elem.tag.endswith('}t'):
                        text += elem.text + ' '
    except Exception as e:
        print(f"Error processing {docx_path}: {e}")
    return text.strip()

def find_idcards(text: str) -> list:
    results = []
    pattern_18 = r'\b(\d{17}[\dXx])\b'
    for match in re.finditer(pattern_18, text):
        id_num = match.group(1)
        if id_num not in results:
            results.append(id_num)
    return results

if __name__ == '__main__':
    all_results = []
    docx_files = list(Path("./docs").rglob('*.docx'))
    total = len(docx_files)
    print(f"找到 {total} 个 Word 文档")
    idcard = []
    for i, filepath in enumerate(docx_files, 1):
        text = extract_text_from_docx(filepath)
        idcards = find_idcards(text)
        if len(idcards) > 0:
            print(f"[{i}/{total}] 扫描: {filepath.name} - 发现 {len(idcards)} 个身份证号")
```

## 关键点

1. **docx 实质是 zip**：包含 `word/document.xml`
2. **隐藏内容**：先打开 word 的"显示/隐藏"选项看是否字体颜色白色隐藏
3. **18 位身份证正则**：`\b(\d{17}[\dXx])\b` 兼容大写 X 校验位
4. **老旧 15 位身份证**：18 位之前的格式
5. **分段存储**：身份证可能用空格、短横分割
