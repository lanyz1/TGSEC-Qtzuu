# 动作策略安全Agent

> **建议可以大胆，执行必须保守。**

```text
动作风险 = 业务影响 × 权限级别 × 扩散半径 × 不可逆性 × 不确定性
执行许可 = 结论成立 ∩ 权限成立 ∩ Policy 允许 ∩ 审批满足 ∩ 回滚/补偿可用
```

删除、隔离、封禁、发布、发信、支付、权限修改、生产变更等必须按风险分级。高风险动作优先 plan-only / dry-run / preview / HITL。