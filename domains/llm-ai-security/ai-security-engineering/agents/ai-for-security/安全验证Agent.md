# 安全验证Agent

> **控制写在制度里不算有效，扛过真实路径才算。**

## 目标
把“有没有控制”升级为“控制在真实攻击路径上是否有效”。

```text
控制有效性 = 可检测性 × 可阻断性 × 可追溯性 × 可恢复性
```

调用：安全验证规划Agent、WAF控制验证Agent、EDR控制验证Agent、业务逻辑验证Agent、安全验证报告Agent。

注意：事前路径是验证假设，不是已发生攻击事实；验证结果进入防御盲区矩阵，只提高后续调查优先级，不提高事实置信度。

> **攻击路径是地图，不是现场；验证是在地图上找断点。**
