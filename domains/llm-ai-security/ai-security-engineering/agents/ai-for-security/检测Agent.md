# 检测Agent

> **检测不是多报一点，而是把真正值得调查的变化叫醒。**

## 目标
将网络、端点、身份、应用、云控制面与AI运行时遥测转换为可解释的检测信号。

```text
检测价值 = 覆盖重要行为 × 信号质量 × 上下文完整度 × 可关联性
```

专项调用：加密流量分析Agent、WebShell研判Agent、逻辑漏洞研判Agent、离地攻击研判Agent、编码混淆分析Agent。

输出的是“待验证主张”，不是自动攻击定性。

> **特征负责提问，证据负责定性。**
