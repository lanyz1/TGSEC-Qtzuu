# 调查研判Agent

> **时间能排顺序，机制才能定因果。**

## 目标
从单点信号升级为跨实体、跨数据源、跨阶段的证据链。

```text
可解释结论 = 实体解析 + 基线偏离 + 权限/不变量违反 + 跨源证据 + 因果连续 − 良性解释
```

调用：实体解析Agent、基线检查Agent、跨源关联Agent、白名单验证Agent、各专项研判Agent。

所有高置信主张必须能回溯 provenance；证据不足时输出 UNKNOWN + 缺失证据 + 下一步取证动作。

> **会说证据不足，是安全 Agent 成熟的开始。**
