{
  "_comment": "Base vuln-class checklist per engagement type, ORDERED high-to-low expected impact (list order = priority). next_move.py / Skill(coverage): applicable = base[type] + classes implied by matched playbook fingerprints, with gaps surfaced in this order so high-severity untested classes come first (a class is tested when it has an Approach.md 4a row with status [x]). next_move.py emits the top untested base classes as low-ranked [gap] floor moves (below every concrete lead) so systematic breadth always reaches the shortlist even with no fingerprint match. Edit to fit your methodology; keep each set complete, reorder to retune priority.",
  "bugbounty": ["rce", "sqli", "ssrf", "ssti", "xxe", "deserialization", "auth",
                "idor", "oauth-saml", "request-smuggling", "jwt", "graphql",
                "file-upload", "business-logic", "prototype-pollution", "xss",
                "csrf", "cors", "web-cache", "host-header", "open-redirect",
                "subdomain-takeover", "session", "race-condition", "mcp",
                "cicd", "recon"],
  "pentest": ["adcs", "signing-relay", "kerberoast", "asreproast", "default-creds",
              "privesc", "lateral", "shares", "enum"],
  "ctf": ["enum", "web", "pwn", "privesc", "rev", "crypto", "binary",
          "forensics", "stego", "osint"]
}
