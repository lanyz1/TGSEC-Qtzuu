# 常见漏洞清单（逐参数比对用）

漏洞挖掘时，对**每个参数**先用本清单逐条比对「适用线索」判断是否适用，然后参考通用威胁建模思路补充适用的漏洞类型：

- **不适用** → 矩阵记 `not_applicable`（简述理由）。
- **适用** → 先在矩阵中创建条目，后续测试完成再更新状态`tested_not_found` / `found` / `doubtful`，并附 payload、现象、判定依据。

> **测试要点**：重点漏洞类型的编号化测试要点见 [test-checkpoints.md](test-checkpoints.md)，下表「测试要点编号」列只列编号；测试结果为 `tested_not_found`/`doubtful`/`filtered` 时须在矩阵 `checkpoint_response` 字段逐编号应答（标 `—` 的类型无强制要点、免填）。

> 同一参数可能同时适用**多个**漏洞——**严禁发现一个就停**，必须把所有适用漏洞逐一测完（破除「一参数一漏洞错误思维」）。
> 漏洞分两类：`generic`（通用）与 `business_logic`（业务逻辑），矩阵条目用 `category` 标注归类。

---

## 一、通用漏洞（category=generic）

| 漏洞类型              | 适用线索                                                 | 测试要点编号  |
| --------------------- | -------------------------------------------------------- | ------------- |
| SQL 注入              | 参数进入查询/排序/过滤；数字或字符串值；后端疑似拼接 SQL | SQL001-003    |
| NoSQL 注入            | 参数进入 Mongo/Redis 等查询；JSON 值                     | —            |
| 命令注入              | 参数进入系统命令（ping/转码/导出/文件名）                | CMDI001-003   |
| 反射型 XSS            | 参数值回显到 HTML/JS 上下文                              | XSS001-002    |
| 存储型 XSS            | 参数被持久化并在他处渲染                                 | XSS001-002    |
| SSRF                  | 参数是 URL/host/ip/回调地址/webhook                      | SSRF001-003   |
| 路径穿越 / 任意文件读 | 参数是文件名/路径/模板名/下载 id                         | PATH001-003   |
| 文件上传              | multipart-file 参数                                      | UPLOAD001-002 |
| XXE                   | 参数是 XML 或接口收 XML                                  | —            |
| SSTI 模板注入         | 参数进入服务端模板                                       | —            |
| 不安全反序列化        | 参数是序列化串（base64/二进制/JSON 带类型）              | —            |
| 开放重定向            | 参数是 redirect/url/next/return                          | —            |
| JWT / Token 缺陷      | 携带 JWT/token 的参数或头                                | —            |
| 敏感信息泄露          | 任意参数/响应                                            | —            |

## 二、业务逻辑漏洞（category=business_logic）

| 漏洞类型                | 适用线索                                       | 测试要点编号 |
| ----------------------- | ---------------------------------------------- | ------------ |
| 水平越权                | 参数含他人可枚举的资源 id（订单/地址/用户 id） | —           |
| 用户枚举                | 所有的登录接口或涉及用户名的接口               | ENUM001-002  |
| 垂直越权 / 未授权       | 管理类/高权限接口；可去登录/降权访问           | —           |
| 金额 / 数量篡改         | 参数是价格/数量/积分/折扣/金额                 | —           |
| 优惠/积分/抽奖重复使用  | 参数含券 id/积分/抽奖/签到                     | PRIZE001     |
| 状态机绕过              | 参数/接口对应业务状态流转                      | —           |
| 并发 / 竞态（TOCTOU）   | 涉及库存/余额/限领/扣减的写操作                | RACE001-003  |
| 重放                    | 携带签名/一次性 token/订单号的请求             | REPLAY001    |
| 验证码 / 短信绕过       | 参数含验证码/短信码                            | SMS001-002   |
| 任意密码重置 / 账号接管 | 找回/重置/绑定流程参数                         | —           |
| 支付逻辑缺陷            | 下单/支付/回调参数                             | —           |
| 业务流程跳跃            | 多步业务可直接跳步                             | —           |
| 回调 / 通知伪造         | 第三方回调/异步通知接口                        | —           |

## 三、通用威胁建模思路

分析每个参数的**参数名、类型、样本值、相关业务功能与约束条件**（来自 url-context 的页面/JS/业务链与 `params/{id}.json`）判断可能的攻击面，威胁建模可以包括但不限于以下维度：

| 维度                   | 深度自问                                                                                                      |
| ---------------------- | ------------------------------------------------------------------------------------------------------------- |
| **数据流**       | 数据从哪进、经过哪些处理、到哪出。有没有地方输入原样进入了数据库/命令/模板？                                  |
| **权限边界**     | 接口有没有校验调用者角色？校验是在哪一层做的（中间件/每个接口/前端）？能不能绕过？                            |
| **资源归属**     | 对象 ID 是数字/有序吗？后端有没有校验"这个 ID 属不属于当前用户"？能不能枚举到别人的资源？                     |
| **状态变更**     | 这个操作会改变什么状态？状态转换有没有前置条件检查？能不能跳过中间状态直达终态？能不能从终态回退？            |
| **客户端可控值** | 金额、数量、概率、折扣、角色、回调 URL 这些"应该服务端决定"的值，客户端能不能传进来覆盖？                     |
| **并发场景**     | 这个操作能不能并发执行？同一资源的并发写会不会产生重复效果（双花、重复领取、超卖）？                          |
| **输出展示**     | 用户输入最终会渲染到哪个页面的哪个元素？用的是 innerHTML 还是 textContent？API 返回的 HTML 字段有没有转义？   |
| **认证与会话**   | 登录/注册/找回密码流程有没有可枚举的差异响应？Token 可不可以伪造或重放？                                      |
| **注入面**       | 输入是否进入 SQL/OS 命令/模板/XML/JSON 路径？转义是充分的吗？有没有二次处理让转义失效？                       |
| **文件操作**     | 上传：类型校验在哪里做、用什么方式、能不能绕过？路径可控吗？下载/删除：路径参数有没有穿越风险？               |
| **业务逻辑**     | 价格/数量可以是负数或 0 吗？优惠/积分/抽奖可以重复使用吗？签名验证的 key 是公开的还是私密的？状态机能逆向吗？ |
